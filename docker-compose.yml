services:
  pulsemation:
    build:
      context: .
      args:
        VITE_N8N_WEBHOOK_URL: ${VITE_N8N_WEBHOOK_URL}
    image: pulsemation-app:latest
    ports:
      - "8081:80"
    env_file:
      - .env
